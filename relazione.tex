\documentclass[12pt,a4paper,oneside]{book}
\usepackage[italian]{babel}
\usepackage{emptypage}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{geometry}
\usepackage{graphicx}
% inserisci altre cartelle dove sono presenti le immagini
\graphicspath{{immagini/}}

\usepackage{calc}
\usepackage{xcolor}
\usepackage{tabularx}
\usepackage{float}


% Imposta un Header migliore:
% numero  nome capitolo a sinistra 
% numero pagina a destra
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE,LO]{\leftmark}
\fancyhead[LE,RO]{\thepage}

\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=magenta,      
    urlcolor=cyan,
    citecolor=black,
    % Imposta Metaparametri per il PDF generato
    % Non lasciare righe bianche: è un errore sintattico
    % Imposta l'autore del PDF
    pdfauthor={AUTORE},
    % Imposta l'argomento della Relazione
    pdfsubject={ARGOMENTO},
    % Imposta il titolo del PDF
    pdftitle={TITOLO},
    % Imposta le parole chiave per il documento
    pdfkeywords={PAROLE, CHIAVE}
}

\usepackage[italian]{cleveref}

\usepackage[backend=biber,
style=alphabetic,
sorting=ynt]{biblatex}
\addbibresource{bibliografia.bib}


% Decommenta per xltabular
%\usepackage{xltabular}
% Decommenta per usare note a pie pagina in tabelle
%\usepackage{tablefootnote}

% Decommenta per usare pseudocodice
%\usepackage{algorithm}
%\usepackage{algorithmic}
%\floatname{algorithm}{Algoritmo}
%\renewcommand{\listalgorithmname}{Elenco degli Algoritmi}

% Decommanenta per usare appendici
% \usepackage{appendix}

% Decommenta per inserire programmi
%\usepackage{listings}

% Decommenta per usare tikz
%\usepackage{tikz}
%\usetikzlibrary{automata, positioning, arrows, shapes}

% Imposta i comandi \Section, \Sref, \Chapter, \insec e \inputchap
\newcommand{\Section}[2]{No section before chapter please}
\newcommand{\Sref}[2][Add Chapter]{Add Chapter}

\newcommand{\Chapter}[2]{
    \chapter{#1}\label{ch:#2}\thispagestyle{empty}
    \renewcommand{\Section}[2]{
        \section{##1}\label{sec:#2:##2}
    }
    \renewcommand{\Sref}[2][#2]{\Cref{sec:##1:##2}}
}


% wrap del comando input che conterrà in automatico la directory attuale
\newcommand{\insec}[1]{Renew this command}

% importa un file tex dalla sottocartela 'algorithms' della cartella dell'attuale capitolo
%\newcommand{\inalgo}[1]{Renew this command}
% riferimento all'ultimo algoritmo importato
%\newcommand{\algoref}{Renew this command}
% label per l'algoritmo auttuale
%\newcommand{\algolbl}{Renew this command}

% ridefinisce i comandi sopra in modo che facciano ciò che ci si aspetta da loro
\newcommand{\setcodedir}[1]{
    \renewcommand{\insec}[1]{\input{#1/##1}}
    %\renewcommand{\inalgo}[1]{\input{#1/algorithms/##1.algo}}
}

% usando questo per importare un capitolo si abilitano i comandi \insec e \inalgo 
\newcommand{\inputchap}[1]{
    \setcodedir{#1}
    %\renewcommand{\algolbl}[1]{\label{algo:#1:##1}}
    \include{#1}
}


\begin{document}

    % Conteggio pagine con numeri romani
    \frontmatter

    %pagina del titolo
    \include{pagina-titolo}
  
    %Indice principale 
    \addtocontents{toc}{\protect\thispagestyle{empty}}
    \tableofcontents
    
    % Deommenta per indice programmi
    %\lstlistoflistings
    
    % Decommenta per indice pseudocodice
    %\addtocontents{loa}{\protect\thispagestyle{empty}}
    %\listofalgorithms

    % Indice delle tabelle
    \addtocontents{lot}{\protect\thispagestyle{empty}}
    \listoftables
    
    % Indice delle figure
    \addtocontents{lof}{\protect\thispagestyle{empty}}
    \listoffigures

    % Conteggio pagine con numeri arabi
    \mainmatter

  

\end{document}